## T05: Problem section（震撼數字）

`components/problem-stats.tsx`（server component）：cream-100 背景，標題「為什麼這件事重要？」。三個數字卡片（grid sm:grid-cols-3）：80 億（一次性飲料杯）、38%（外送訂單佔比）、<3%（進入循環系統）。每張卡片含大型 serif 數字（text-5xl/6xl）+ 標籤（uppercase tracking-widest text-forest-500）+ 說明文字。底部加「* 數字為估計值」免責說明。

狀態：完成
