## T04: Hero section（含主標語與 email CTA）

`components/hero.tsx`（server component）：全螢幕高度（min-h-screen）cream-50 背景，含極輕微 SVG grain texture overlay（opacity 3%）。主標語「讓每一個容器，都被看見它的循環價值。」使用 serif 字體、forest-900 色。副標與說明文字、候補名單 badge（forest-500 點 + pulse 動畫）。嵌入 `<EmailForm source="hero" variant="light" />`，下方輔助文案「一個 email 就能在公測時搶先試用」。

狀態：完成
